HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# Where the Next.js app lives. Used to build the password-reset link that Strapi
# emails out; seeded into users-permissions advanced settings on boot.
FRONTEND_URL=http://localhost:3000

# Password-reset email delivery. Without SMTP_HOST, Strapi falls back to the
# `sendmail` provider, which will not deliver from a droplet — bootstrap() warns.
#
# Port 465 uses implicit TLS; 587 uses STARTTLS. config/plugins.ts picks based on
# the port. EMAIL_FROM's domain must publish SPF (and ideally DKIM) or the mail is
# rejected: commonnotebook.com has DMARC p=reject, so a misaligned message is
# refused outright rather than spam-foldered.
#
# Verify with:  node scripts/test-email.js --to you@example.com
SMTP_HOST=smtp.forwardemail.net
SMTP_PORT=465
SMTP_USERNAME=robot@commonnotebook.com
SMTP_PASSWORD=
EMAIL_FROM=robot@commonnotebook.com
EMAIL_FROM_NAME="Common Notebook"
# Where replies go. Leave blank to reply to EMAIL_FROM.
EMAIL_REPLY_TO=

# Access-token lifespan in seconds. Only override for testing expiry.
# ACCESS_TOKEN_LIFESPAN=1800

AWS_ACCESS_KEY_ID=tobemodified
AWS_ACCESS_SECRET=tobemodified
AWS_REGION=tobemodified
AWS_BUCKET=tobemodified
